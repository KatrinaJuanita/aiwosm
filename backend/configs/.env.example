# WOSM Go Backend 环境变量配置示例
# 复制此文件为 .env 并根据实际环境修改配置

# 应用环境 (dev/test/prod)
APP_ENV=dev

# 数据库配置
DB_HOST=localhost
DB_PORT=1433
DB_NAME=wosm
DB_USER=sa
DB_PASSWORD=PWD

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT配置
JWT_SECRET=your-super-strong-jwt-secret-key-min-32-chars

# 文件上传配置（可以是相对路径或绝对路径）
# 相对路径示例: ./uploads/prod
# 绝对路径示例: /opt/wosm/uploads
UPLOAD_PATH=./uploads/prod

# SSL证书配置（生产环境）
SSL_CERT=/etc/ssl/certs/wosm.crt
SSL_KEY=/etc/ssl/private/wosm.key

# 前端URL（生产环境CORS配置）
FRONTEND_URL=https://your-domain.com

# 监控配置
JAEGER_ENDPOINT=http://localhost:14268/api/traces

# 日志配置
LOG_LEVEL=info
LOG_PATH=logs/wosm.log

# 服务端口
SERVER_PORT=8080
